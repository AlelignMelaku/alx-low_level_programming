#include <stdio.h>
#include <math.h>
#include "main.h"
/**
 * mul - multiple of two intege
 * @a: this is first integer a
 * @b: this is also the second integer b
 * Return: muliplication of a and b
 */
int mul(int a, int b)
{
	mul = a * b;
	return (mul);
}
